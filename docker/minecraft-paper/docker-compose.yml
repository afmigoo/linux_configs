services:
  server:
    container_name: mc-vanilla
    build: .
    image: mc-vanilla:latest
    volumes:
      - ./data:/data
    environment:
      JAVA_OPTS: "-Xms3G -Xmx3G"
    ports:
      - 20336:25565

